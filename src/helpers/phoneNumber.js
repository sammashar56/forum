export default value => {
    if (value[0] === "0") {
      let number = value.substring(1, value.length);
  
      const validNumber = "254" + number;
  
      return validNumber;
    } else {
      const validNumber = "254" + number;
  
      return validNumber;
    }
  };
  